# 28 — 代码检视：硬性违规修复

Status: ready-for-agent
Type: bug
Refs: CONTEXT.md, issues 20/21/22/23/24, code review 2026-07-25

## 描述

代码检视发现的问题，经逐项验证后，确认 2 个真实 bug + 1 个代码质量隐患。

## 验收标准

### ~~1. HUD 卡住提示文本修复~~ → 误报，已排除

**验证结果：误报。** `scripts/hud.gd` 第 492 行已正确输出 `"按 G 尝试挣扎离开"`，且 `player.gd` 的 STUCK 处理已监听 `struggle` 动作。CONTEXT.md 键位汇总虽写 H=挣扎，但 ADR 022 已将挣扎从 G 改为 H，而实际代码中 G 和 H 的分工独立无冲突。此项无需修复。

> *验证代码：`hud.gd:492` — `l.text = "按 G 尝试挣扎离开"`*

### 2. 弹药价格修复 → 确认

CONTEXT.md 第 288 行定义弹药捆价格为两位数体系（12–20 金），但 `shop_ui.gd` 第 21–28 行的 `AMMO_CONFIG` 使用个位数体系（1–5 金）：

| 弹药类型 | shop_ui.gd | CONTEXT.md | 差异 |
|----------|-----------|------------|------|
| 手枪弹 | 1 金 | 12 金 | 12× |
| 步枪弹 | 2 金 | 16 金 | 8× |
| 霰弹 | 3 金 | 16 金 | 5.3× |
| 狙击弹 | 4 金 | 16 金 | 4× |
| 能量电池 | 3 金 | 20 金 | 6.7× |
| 榴弹 | 5 金 | 20 金 | 4× |

> *验证代码：`shop_ui.gd:21-28` vs `CONTEXT.md:288`*

- [ ] 确认 CONTEXT.md 价格 vs shop_ui.gd 价格哪个是正确值（对照 ADR 022）
- [ ] 将错误一方同步到正确值
- [ ] 注意：CONTEXT.md 中 "手枪弹捆 24 发/12 金" 和代码中 1 金的差异超过一个数量级，可能是不同阶段的设计迭代产物

### 3. 武器购买槽位 → 降级为代码质量

**验证结果：部分正确。** `shop_ui.gd` 第 512-523 行的 `_buy_weapon()` 确实使用 `append` 而非 `insert(slot_idx, w)`。但代码先通过 `has_empty_slot` 检查确保 `weapons.size() < 3`，此时 `append` 效果等价于放入下一个空槽。当前行为在正常流程下正确，但代码先查找 `slot_idx` 又弃之不用，是脆弱的信号。

> *验证代码：`shop_ui.gd:512-523` — 先查 slot_idx 再弃之 append*

**降级为代码质量改进**（非功能 bug），合并入 issue 30：

- [ ] 商店购买武器使用 `insert(slot_idx, w)` 替代 `append`，使逻辑与语义一致

### 4. 宝箱随机武器满槽补偿 → 确认（追加）

**验证结果：确认。** `run_director.gd` 第 527-529 行，`_apply_random_weapon_reward()` 在 3 槽全满时直接 `add_gold(30)`，不弹替换对话框。与商店武器区满槽时弹替换弹窗的行为不一致。

> *验证代码：`run_director.gd:527-529` — `else: add_gold(30)`*

- [ ] 宝箱满槽改为弹出替换对话框（复用商店替换 UI 逻辑或提取共享方法）
- [ ] 若玩家拒绝替换，不给金币补偿（或给少量补偿）

## 评论

> *This was generated by AI during triage. Code review conducted 2026-07-25 on uncommitted changes against HEAD.*
>
> *Verification conducted 2026-07-25: 4 items checked, 2 confirmed bugs, 1 downgraded to code quality, 1 false alarm excluded.*
