# 05 — shop_ui.gd 重构（去重 + 死代码清理）

**Status:** done

**Blocked by:** 04

**构建内容：** 重构 `shop_ui.gd` 消除代码审查发现的重复代码和死代码，提升可维护性。商店对玩家的功能行为不变。

**验收标准：**

### 三区按钮状态刷新去重

- [ ] `_refresh_all_button_states()` 中武器区/弹药区/手雷区的三套遍历-匹配-禁用逻辑提取为参数化方法
- [ ] 参数：按钮名标识、额外条件回调（手雷区需要上限检查）
- [ ] 重构后当前 ~100 行压缩到 ~30 行，三个区调用同一方法
- [ ] 重构后金币不足时按钮正确置灰、手雷满上限时按钮正确置灰——行为不变

### 标签刷新去重

- [ ] `_refresh_ammo_zone_reserve_labels()` 和 `_refresh_grenade_zone_count_labels()` 合并为一个参数化方法
- [ ] 参数：配置字典引用、player 数据访问路径
- [ ] 重构后行为不变

### 死代码清理

- [ ] 删除 `_get_rng()` 中 `return RandomNumberGenerator.new()` 兜底分支（RunDirector 总是有 `rng`，此分支永不可达）
- [ ] 保留 `_run_director.rng` 主路径

### 审慎决策：商店武器耐久条

- [ ] **推荐保留**。虽然 issue 22 spec 未要求，但耐久条对购买决策有实际价值（玩家能看到新枪的耐久上限），且无副作用
- [ ] 若决定移除，将相关代码从 shop_ui.gd 中删除

## 评论

（评论与对话历史追加于此，新内容置于最前。）
